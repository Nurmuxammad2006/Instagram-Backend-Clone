package com.example.instagrambackend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class HttpsConfig {
    // HTTP to HTTPS redirect handled by Nginx in production
}